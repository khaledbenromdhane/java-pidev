<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<!-- ═══════════════════════════════════════════════════════════════
     Frontoffice – Événement List
     Converted from: evenement/index.html.twig
     ═══════════════════════════════════════════════════════════════ -->
<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.pidev.controllers.EvenementListController"
            stylesheets="@/com/pidev/styles/admin.css" styleClass="app-root">
    <top>
        <VBox>
            <!-- Page Header -->
            <HBox styleClass="page-header" spacing="16" alignment="CENTER_LEFT">
                <padding>
                    <Insets top="24" right="32" bottom="24" left="32"/>
                </padding>
                <VBox spacing="6" HBox.hgrow="ALWAYS">
                    <Label text="📅  Événements" styleClass="page-title"/>
                    <Label text="Gérez tous les événements de la plateforme" styleClass="subtitle"/>
                </VBox>
                <Button text="+ Ajouter un Événement" styleClass="btn-admin-primary" onAction="#onAdd"/>
            </HBox>

            <!-- Search Bar -->
            <HBox spacing="12" alignment="CENTER_LEFT"
                  style="-fx-background-color: rgba(255,255,255,0.04); -fx-border-color: rgba(212,175,55,0.15); -fx-border-radius: 10; -fx-background-radius: 10;">
                <padding>
                    <Insets top="12" right="24" bottom="12" left="24"/>
                </padding>
                <Label text="🔍" styleClass="text-muted"/>
                <TextField fx:id="searchField" promptText="Rechercher par nom, lieu, type, description…"
                           styleClass="search-input" HBox.hgrow="ALWAYS"/>
                <Button text="🔍 Rechercher" styleClass="btn-admin-primary" onAction="#onSearch"
                        style="-fx-padding: 8 16; -fx-font-size: 12px;"/>
                <Button fx:id="resetBtn" text="✕ Effacer" styleClass="btn-ghost" onAction="#onReset"/>
            </HBox>
        </VBox>
    </top>

    <center>
        <VBox>
            <padding>
                <Insets top="16" right="24" bottom="24" left="24"/>
            </padding>

            <!-- Search info label -->
            <Label fx:id="searchInfoLabel" text="" styleClass="text-muted"
                   style="-fx-padding: 0 0 10 0; -fx-font-size: 12px;" visible="false" managed="false"/>

            <!-- Table Card -->
            <VBox styleClass="admin-table-card" VBox.vgrow="ALWAYS">
                <TableView fx:id="table" VBox.vgrow="ALWAYS">
                    <columnResizePolicy>
                        <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                    </columnResizePolicy>
                    <columns>
                        <TableColumn fx:id="colId" text="#" prefWidth="60" sortable="true"/>
                        <TableColumn fx:id="colNom" text="Nom" prefWidth="180" sortable="true"/>
                        <TableColumn fx:id="colType" text="Type" prefWidth="140" sortable="true"/>
                        <TableColumn fx:id="colParticipants" text="Participants" prefWidth="100" sortable="true"/>
                        <TableColumn fx:id="colDate" text="Date" prefWidth="110" sortable="true"/>
                        <TableColumn fx:id="colHeure" text="Heure" prefWidth="80" sortable="true"/>
                        <TableColumn fx:id="colLieu" text="Lieu" prefWidth="140" sortable="true"/>
                        <TableColumn fx:id="colPaiement" text="Paiement" prefWidth="100" sortable="true"/>
                        <TableColumn fx:id="colDescription" text="Description" prefWidth="200" sortable="false"/>
                        <TableColumn fx:id="colActions" text="Actions" prefWidth="130" sortable="false"/>
                    </columns>
                </TableView>
            </VBox>
        </VBox>
    </center>
</BorderPane>
